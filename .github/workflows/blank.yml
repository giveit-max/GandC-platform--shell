# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:export default function Home() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <section style={{ textAlign: "center", marginBottom: "3rem" }}>
        <h1>G&C — Your Community. Your Power.</h1>
        <p>
          Buy, sell, book, and donate — all in one transparent platform built for real people and real impact.
        </p>
        <button>Join the Marketplace</button>
      </section>

      <section>
        <h2>What We Do</h2>
        <ul>
          <li>Buy & Sell Locally</li>
          <li>Book Services Instantly</li>
          <li>Donate With Transparency</li>
          <li>Track Every Dollar</li>
        </ul>
      </section>

      <section>
        <h2>G&C Wallet</h2>
        <p>
          Your money, your community, one wallet. Secure payments, fast payouts, and verified donations.
        </p>
      </section>

      <section>
        <h2>Transparency Map</h2>
        <p>
          See your community clearly — hospital stability scores, verified data, and real‑time updates.
        </p>
      </section>

      <section>
        <h2>Government Dashboard</h2>
        <p>
          Built for public benefit partners — case verification, donation routing, and predictive alerts.
        </p>
      </section>

      <footer style={{ textAlign: "center", marginTop: "4rem" }}>
        <p>© 2026 G&C Platform — Hollywood, FL</p>
      </footer>
    </main>
  );
}

    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
